// procedure push(n,v:integer);

using namespace std;

int main()
{

While ( robot[n] )
  {
   if (ram[71] >= stack_base) || (ram[71] < (stack_base + stack_size))
   {
     ram[ram[71]] = v;
     inc(ram[71]);
   else
   {
       robot_error(n,1,cstr(ram[71]));
   }

   }
  }
}
