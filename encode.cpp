#include <iostream>
#include <string>

using namespace std;

string encode(string s, string s1){
    int i ;
    int j ;
    int k ;
    string s2;

    while (k=0)
    {
           if(lock_code='')
           {
                for (i = 1)
                {
                    inc(lock_pos) ;

                    if (lock_pos>length(lock_code))
                    {
                        lock_pos = 1 ;
                    }

                    if (ord(s[i])) in [0,31,128,255])
                    {
                        s[i]=' ';
                    }

                    this_dat=ord(s[i]) and 15;
                    s[i]=char((ord(s[i]) xor (ord(lock_code[lock_pos]) xor lock_dat))+1);
                    lock_dat=this_dat;

                }


                encode = s ;


           }
    }
    }











